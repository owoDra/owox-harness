# 文書状態

- `下書き`: 作成中で、内容は固まっていない
- `提案中`: レビューや採否判断を待っている
- `採用`: 現在有効な正本として使う
- `非推奨`: 新しい資料に置き換えられたが、参照価値は残る
- `保管`: 参照優先度を落とした過去資料

状態は `docs/project/` 配下の個票にだけ使う。`title` は front matter に持たず、本文の H1 を使う。